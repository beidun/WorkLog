# Worklog Agent 设计

## 目标

Worklog Agent 将本地对话和工具事件转换成可追溯的会话事实、工作事项和项目进度。模型只负责在受限证据上推理，不能创造事件、修改原始记录或绕过状态约束。

## 三级编排

```text
扫描器
  -> 会话 Agent: observe -> plan -> reason -> verify -> commit
  -> 工作事项聚合（跨会话、跨时间段）
  -> 工作事项 Agent: 对每个聚合事项重新判断状态与结论
  -> 项目 Agent: 基于事项证据重新判断项目叙事
  -> 进度快照 / Web / 日报
```

### 会话 Agent

- `observe`：接收扫描器产生的真实事件，识别用户目标、对话尾部、工具调用和成功/失败结果。
- `plan`：生成重点事件 ID、待回答问题和硬约束。计划不会替代证据，只负责缩小推理范围。
- `reason`：将基线摘要、计划和脱敏事件发送给 Provider，要求返回结构化 Digest。
- `reason` 是会话语义主路径：除摘要字段外，Provider 应返回逐条绑定真实 event ID 的 `facts`（finding/change/validation/risk/next_step），让后续事项聚合消费模型理解后的原子事实。
- `verify`：检查所有 `evidenceIds` 是否存在、是否与计划有交集、状态是否允许跃迁、开放回合是否误报完成、阻塞/待办是否与结果一致。
- `commit`：只有验证通过才写入 `session_digests`、`session_facts` 和证据关联表。

Provider 不是三层共用一个无上下文的提示词：调用会显式携带 `role=session`，并使用 Session Agent 提示词，要求提取单个会话的目标、原子事实、验证、阻塞和下一步。

模型失败、超时、截断 JSON 或事实引用非法时，保留确定性 Digest，并将失败写入 `agent_runs` / `agent_run_steps`。启用 Provider 时，确定性结果只作为基线和安全门；模型返回的语义字段与原子 facts 优先展示。

### 项目 Agent

项目 Agent 不读取原始文件，而是读取已经归属项目的工作事项和真实事件证据。观察阶段保留真实事件类型（用户消息、助手消息、工具调用、工具结果），避免把不同角色的文本混成一类。它使用同一套证据门控，输出项目级 headline、summary、完成项、验证项、阻塞、剩余工作、stage、下一步和引用。

项目决策写入 `project_agent_decisions`，输入哈希覆盖事项状态、摘要、下一步、证据 ID、事件类型、时间和文本，输入不变时跳过重复调用。项目 Agent 失败不会覆盖确定性项目统计，只会保留上一份有效 Agent 叙事或使用确定性叙事。项目级事实和下一步与会话级、事项级 Agent 轨迹一起脱敏持久化；轨迹带有 `scope=project/work_item/session`、`projectId` 和可选 `workItemId`，可区分三级 Agent。

调用会显式携带 `role=project`，项目提示词要求综合多个事项、识别当前主线、优先阻塞和剩余工作，并禁止凭空创建事项。

### 工作事项 Agent

工作事项 Agent 位于会话事实和项目叙事之间。它读取一个聚合事项关联的真实事件、分段事实和确定性基线，重新生成事项标题、摘要、状态、验证、阻塞、剩余工作和下一步。模型决策写入 `work_item_agent_decisions`，按事项输入哈希缓存；扫描额度独立于会话和项目额度，超额事项进入下一轮。人工纠正优先于模型决策，模型失败时保留确定性事项。事项 Agent 的状态会被项目 Agent 和总览统计消费，因此项目计数不再只由规则聚合决定。

读取侧也会重新验证事项决策的输入哈希和证据归属；项目决策必须引用当前项目的真实事件，且不能早于项目最新活动。工作事项重建后会清理已消失 ID 的旧决策，避免历史模型叙事泄漏到新聚合事项。

调用会显式携带 `role=work_item`，事项提示词要求跨会话合并同一目标、处理矛盾证据，并禁止将单个读取命令成功当成完成。

## 状态不变量

- `verified` 必须有成功验证、明确结论或完整的只读核查证据。
- 单纯成功读取文件、列目录等工具结果不构成完成证据；工具结果必须明确包含验证/测试成功语义，或由 `task_completed` / 明确结论支撑。
- `openTurn=true` 时只能是 `in_progress` 或 `partially_done`。
- `blocked` 必须有引用事件明确描述阻塞原因。
- `verified` 不能同时存在 blocker、remaining 或 nextStep；否则降级为 `done_unverified`。
- 所有模型结论必须能回溯到事件 ID；展示层只展示脱敏后的证据。
- 项目概览的“正在推进”只计算最近 30 天有活动的事项，历史事项仍保留在详情页。
- 工作事项 Agent 的 `verified` 仍必须引用成功工具结果、`task_completed` 或明确完成结论；普通进行中话术不能升级状态。
- 项目 Agent 的 stage 受事项聚合结果约束：无开放事项的已完成项目不能被模糊的模型状态重新打开；存在阻塞时始终保留阻塞阶段。

## 扫描顺序

1. 增量读取 Codex / Claude Code JSONL。
2. 清理注入、命令回显和敏感字段。
3. 归属项目并重建会话 Digest。
4. 优先为未完成、阻塞和待验证会话调用模型；超出额度的会话进入下一轮队列。
5. 重建工作事项和跨会话工作主线。
6. 对工作事项执行工作事项 Agent；按阻塞/待验证/推进中优先、输入哈希缓存和独立额度调度，人工纠正跳过模型。
7. 对项目执行项目 Agent，调度优先级为阻塞/待验证/推进中，再按最近活动排序；按输入哈希缓存，项目级调用有独立额度，超过额度的项目进入下一轮，不会覆盖确定性统计。
8. 捕获仓库快照和进度变化。

## 隐私与回退

远程 Provider 必须显式启用 HTTPS 和远程隐私授权。发送前会遮蔽 API Key、Bearer Token、密码、Cookie，并将本地主目录替换为 `$HOME`。连接测试只使用合成事件；真实历史增强需要用户明确允许数据出站。
